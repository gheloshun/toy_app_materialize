<% if object.errors.any? %>
  <div id="error_explanation">
    <h6><%= pluralize(object.errors.count, "error") %> prohibited this user from being saved:</h6>

    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
